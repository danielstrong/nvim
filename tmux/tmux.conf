# ==========
#  System
# ==========
bind -N "localleader prefix" \\ switch-client -T localleader
bind -N "Copy CWD to clipboard" -T localleader Y run-shell 'tmux set-buffer "#{pane_current_path}" ; echo "#{pane_current_path}" | pbcopy ; tmux display "Copied: #{pane_current_path}"'
bind -N "Toggle Mouse" -T localleader c set-option mouse
bind -N "Toggle Status bar" -T localleader f set-option status
bind -N "Display info" -T localleader g display "Terminal: #{client_termname} "
bind -N "Display window information" -T localleader i display-message
bind -N "Show a clock" -T localleader a clock-mode
bind -N "Chooser Buffer to paste" -T localleader ] choose-buffer
#bind -N "Refresh Client"   R                    refresh-client
bind -N "Edit Tmux Config" - new-window -n "tmux.conf" bash -c 'vim ~/.config/tmux/tmux.conf && tmux source ~/.config/tmux/tmux.conf'
bind -N "Reload Tmux Config" + source-file ~/.config/tmux/powerline-theme.conf 
bind -N "Reload Tmux Config" _ source-file ~/.config/tmux/tmux.conf 
bind -N "Lookup key" "Q" command-prompt -k -p key "list-keys -1N \"%%%\""
bind -N "List keys"  q list-keys -N
bind -N "List keys" -T localleader 'q' list-keys -Na -T localleader
bind -N "List keys" -T sessionleader 'q' list-keys -Na -T sessionleader
bind -N "List keys" -T tableader 'q' list-keys -Na -T tableader
bind -N "List keys" -T windowleader 'q' list-keys -Na -T windowleader

# ==========
#  Sessions
# ==========
bind -N "Session prefix" e switch-client -T sessionleader
bind -N "Kill Session" -T sessionleader x confirm-before -p "kill session #S? (y/n)" "kill-session"
bind -N "Select Next Session" -T sessionleader -r l switch-client -n
bind -N "Select Next Session" -T sessionleader -r j switch-client -n
bind -N "Select Next Session" -T sessionleader -r h switch-client -p
bind -N "Select Next Session" -T sessionleader -r k switch-client -p
bind -N "Select Session 1" -n M-1 select-window -t 1
bind -N "Select Session 2" -n M-2 select-window -t 2
bind -N "Select Session 3" -n M-3 select-window -t 3
bind -N "Select Session 4" -n M-4 select-window -t 4
bind -N "Select Session 5" -n M-5 select-window -t 5
bind -N "Select Session 6" -n M-6 select-window -t 6
bind -N "Select Session 7" -n M-7 select-window -t 7
bind -N "Select Session 8" -n M-8 select-window -t 8
bind -N "Select Next Session" -r ) switch-client -n
bind -N "Select Prev Session" -r ( switch-client -p
bind -N "Choose a session from a list" o choose-tree -ZsK '#{?#{e|<:#{line},9},#{e|+:1,#{line}},#{?#{e|<:#{line},35},M-#{a:#{e|+:97,#{e|-:#{line},9}}},}}'
bind -N "Choose a session from a list" -T sessionleader f choose-tree -ZsK '#{?#{e|<:#{line},9},#{e|+:1,#{line}},#{?#{e|<:#{line},35},M-#{a:#{e|+:97,#{e|-:#{line},9}}},}}'
bind -N "Open Session or Start Session" -T sessionleader F run-shell "tmux neww ~/.config/tmux/create-session.sh"
bind -N "Detach the current client" D detach-client
bind -N "Rename Session" R command-prompt -I "#S" "rename-session -- '%%'"

# ==========
#  Tabs
# ==========
bind -N "Tab prefix" t switch-client -T tableader
bind -N "Select Next Tab" -T tableader -r l next-window
bind -N "Select Next Tab" -T tableader -r j next-window
bind -N "Select Prev Tab" -T tableader -r h previous-window
bind -N "Select Prev Tab" -T tableader -r k previous-window
# bind -N "Select Next Tab" -r o next-window
# bind -N "Select Prev Tab" -r i previous-window
bind -N "Swap Tab Next" -T tableader -r 'L' 'swap-window -t +1;select-window -t +1'
bind -N "Swap Tab Prev" -T tableader -r 'H' 'swap-window -t -1;select-window -t -1'
bind -N "Swap Tab Next" -T tableader -r 'J' 'swap-window -d -t +1'
bind -N "Swap Tab Prev" -T tableader -r 'K' 'swap-window -d -t -1'
bind -N "Kill Tab" -T tableader x confirm-before -p "kill window #I (#W)? (y/n)" "kill-window"
bind -N "Choose Tab" p choose-window
bind -N "Choose Tab" -T tableader f choose-window
bind -N "Search for Tab" -T tableader F command-prompt "find-window '%%'"
bind -N "Swap Tab Prev" -r '{' 'swap-window -d -t -1'
bind -N "Swap Tab Next" -r '}' 'swap-window -d -t +1'
bind -N "Select Tab 1" -n F1 select-window -t 1
bind -N "Select Tab 2" -n F2 select-window -t 2
bind -N "Select Tab 3" -n F3 select-window -t 3
bind -N "Select Tab 4" -n F4 select-window -t 4
bind -N "Select Tab 5" -n F5 select-window -t 5
bind -N "Select Tab 6" -n F6 select-window -t 6
bind -N "Select Tab 7" -n F7 select-window -t 7
bind -N "Select Tab 8" -n F8 select-window -t 8
bind -N "Change to last Tab" K last-window
bind -N "Create Tab at session root" c new-window -c "#{pane_current_path}"
bind -N "Create Tab at current" C new-window 
bind -N "Toggle between smallest and largest Tab sizes if Tab visible in multilpe places" F set -w window-size
bind -N "Rename Tab" r command-prompt -p "Rename Tab: " "rename-window \"%%\""

# ==========
#  Windows
# ==========
bind -N "Window prefix" w switch-client -T windowleader
bind -N "Select Window above" -T windowleader -r k select-pane -U
bind -N "Select Window below" -T windowleader -r j select-pane -D
bind -N "Select Window left" -T windowleader -r h select-pane -L
bind -N "Select Window right" -T windowleader -r l select-pane -R
bind -N "Select Next Tab" -r O select-pane -t :.+
bind -N "Select Prev Tab" -r I select-pane -t :.-
bind -N "Swap Window above" -T windowleader -r K swap-pane -U
bind -N "Swap Window below" -T windowleader -r J swap-pane -D
bind -N "Swap Window left" -T windowleader -r H swap-pane -U
bind -N "Swap Window right" -T windowleader -r L swap-pane -D
bind -N "Swap Window with First Window" -T windowleader n if '[ #{pane_index} -eq 1 ]' 'swap-pane -s "!"' 'select-pane -t:.1 ; swap-pane -d -t 1 -s "!"'
bind -N "Window Layout Even Horizontal" -T windowleader 1 select-layout even-horizontal
bind -N "Window Layout Even Vertical" -T windowleader 2 select-layout even-vertical
bind -N "Window Layout Main Horizontal"  -T windowleader 3 select-layout main-horizontal
bind -N "Window Layout Main Vertical" -T windowleader 4 select-layout main-vertical
bind -N "Window Layout Tiled" -T windowleader 5 select-layout tiled
bind -N "Window Layout Next" -T windowleader -r 0 next-layout
bind -N "Split Window horizontally" -T windowleader s split-window -v -c "#{pane_current_path}"
bind -N "Split Window vertically" -T windowleader S split-window -h -c "#{pane_current_path}"
bind -N "Split Window vertically" -T windowleader v split-window -h -c "#{pane_current_path}"
bind -N "Kill Window" -T windowleader x confirm-before -p "kill-pane #P? (y/n)" kill-pane
bind -N "Split Window vertically" S split-window -h -c "#{pane_current_path}"
# bind -N "Split Window vertically" V split-window -b -h -c "#{pane_current_path}"
bind -N "Split Window horizontally" s split-window -v -c "#{pane_current_path}"
# bind -N "Split pane horizontally" N split-window -b -v -c "#{pane_current_path}"
# bind -N "Split pane vertically" N split-window -h -c "#{pane_current_path}"
# bind -N "Split pane horizontally" n split-window -v -c "#{pane_current_path}"
# bind -N "Swap Window prev" -r '[' swap-pane -U
# bind -N "Swap Window next" -r ']' swap-pane -D
bind -N "Select Window 1" -n C-F1 select-pane -t 1
bind -N "Select Window 2" -n C-F2 select-pane -t 2
bind -N "Select Window 3" -n C-F3 select-pane -t 3
bind -N "Select Window 4" -n C-F4 select-pane -t 4
bind -N "Select Window 5" -n C-F5 select-pane -t 5
bind -N "Select Window 6" -n C-F6 select-pane -t 6
bind -N "Select Window 7" -n C-F7 select-pane -t 7
bind -N "Select Window 8" -n C-F8 select-pane -t 8
bind -N "Select Window above" -r "up" select-pane -U
bind -N "Select Window below" -r "down" select-pane -D
bind -N "Select Window left" -r "left" select-pane -L
bind -N "Select Window right" -r "right" select-pane -R
bind -N "Choose Window" i display-panes -d 0
bind -N "Choose Window" -T windowleader f display-panes -d 0
bind -N "Change to last Window" J last-pane
bind -N "Resize Window up" -r C-k resize-pane -U 5
bind -N "Resize Window down" -r C-j resize-pane -D 5
bind -N "Resize Window left" -r C-h  resize-pane -L 5
bind -N "Resize Window right" -r C-l  resize-pane -R 5
bind -N "Resize Window up" -r C-up resize-pane -U 5
bind -N "Resize Window down" -r C-down resize-pane -D 5
bind -N "Resize Window left" -r C-left  resize-pane -L 5
bind -N "Resize Window right" -r C-right  resize-pane -R 5
bind -N "Spread Window out evenly" C-Space select-layout -E
bind -N "Spread Window out evenly" "=" select-layout -E
# bind -n S-Up move-pane -h -t '.{up-of}'
# bind -n S-Right move-pane -t '.{right-of}'
# bind -n S-Left move-pane -t '.{left-of}'
# bind -n S-down move-pane -h -t '.{down-of}'

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

bind-key -N "Break Window" , break-pane
bind-key -N "Send Window to Tab" . command-prompt -p "Send window to tab: "  "join-pane -t :'%%'"
bind-key -N "Send Tab to Session" > command-prompt -p "Send tab to session: "  "move-window -t '%%':"
#bind-key -N "Find Window" s command-prompt "find-window -Z -- '%%'"
#bind-key    -T prefix       ,                    command-prompt -I "#W" "rename-window -- '%%'"
#bind-key    -T prefix       .                    command-prompt -T "move-window -t '%%'"

# ==========
#  Copy ModNe
# ==========
unbind -n MouseDrag1Pane
bind -n MouseDrag1Pane select-pane

bind -N "Search Down" '/'  copy-mode \; command-prompt -T search -p "(search down)" { send-keys -X search-forward "%%" }
bind -N "Search Up" '?'    copy-mode \; command-prompt -T search -p "(search up)" { send-keys -X search-backward "%%" }

bind -N "Paste contents from buffer" ] paste-buffer

bind -N "Copy Mode" [ copy-mode
bind -N "Copy Mode down" j copy-mode\; send-keys -X cursor-down
bind -N "Copy Mode up" k copy-mode\; send-keys -X cursor-up
bind -N "Copy Mode left" h copy-mode\; send-keys -X cursor-left
bind -N "Copy Mode right" l copy-mode\; send-keys -X cursor-right
bind -N "Copy Mode half page down" d copy-mode\; send-keys -X halfpage-down
bind -N "Copy Mode half page up" u copy-mode\; send-keys -X halfpage-up
bind -N "Half Page Down" -T copy-mode-vi d send-keys -X halfpage-down
bind -N "Half Page Up" -T copy-mode-vi u send-keys -X halfpage-up

bind -N "Visual Mode" v copy-mode\; send-keys -X begin-selection
bind -N "Visual Line Mode" V copy-mode\; send-keys -X select-line
bind -N "Visual Rectangle Mode" C-v copy-mode\; send-keys -X begin-selection\; send-keys -X rectangle-toggle
bind -N "Visual Word Mode"  C-s copy-mode\; send-keys -X select-word
bind -N "Toggle Visual Mode" -T copy-mode-vi v send-keys -X begin-selection
bind -N "Toggle Visual Line Mode" -T copy-mode-vi V send-keys -X select-line
bind -N "Toggle Visual Rectangle Mode" -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind -N "Toggle Visual Word Mode" -T copy-mode-vi C-s send-keys -X select-word


# ==========
#  Settings
# ==========
#set-option status off
#set-environment -g FZF_TMUX 1  # supposed to make fzf show in a tmux pane doesnt seem to work here
set -g mouse on
setw -g aggressive-resize off
setw -g mode-keys vi
set -g status-keys vi
#set -g status-keys emacs
set -g base-index 1
set -wg pane-base-index 1

set -g repeat-time 750
set -g display-time 2000 # tmux message dispaly duraiton
set -g display-panes-time 2000
setw -g monitor-activity on
setw -g activity-action none
setw -g renumber-windows on
set -g visual-activity off
set -g visual-bell off
set -g focus-events on
#set -g bell-action none

set -g status-interval 30 #frequency in seconds to refresh status panel

# ==========
# Theme
# ==========
# ----------------------------------
# Classic Green colors for Tmux
# ----------------------------------
# set -g @primary_color green
# set -g @primary_bg_color color234
# set -g @active_color color237
# set -g @command_color yellow
# set -g @panes_color brightyellow
# set -g @panes_active_color brightwhite
# set -g @bell_fg_color black
# set -g @bell_bg_color red

# ----------------------------------
# Kanagawa Dragon colors for Tmux
# ----------------------------------
set -g @primary_color "#8a9a7b"
set -g @primary_bg_color "#0d0c0c"
set -g @active_color "#2d2b2a"
set -g @command_color "#c4b28a"
set -g @panes_color "#b6927b"
set -g @panes_active_color "#c5c9c5"
set -g @bell_fg_color "#0d0c0c"
set -g @bell_bg_color "#c4746e"

# ----------------------------------
# Rose Pine colors for Tmux
# ----------------------------------
# set -g @primary_color "#286983"
# set -g @primary_bg_color "#1f1d2e"
# set -g @active_color "#26233a"
# set -g @command_color "#f6c177"
# set -g @panes_color "#c4a7e7"
# set -g @panes_active_color "#e0def4"
# set -g @bell_fg_color "#191724"
# set -g @bell_bg_color "#eb6f92"

# https://github.com/arcticicestudio/nord-tmux/blob/develop/src/nord-status-content.conf
# https://www.ditig.com/256-colors-cheat-sheet

set -g status-position bottom
set-option -g status-justify left
set -g set-titles on
set -g set-titles-string "[#S] #I:#W"
set -g status-style "fg=#{@primary_color},bg=#{@primary_bg_color}"    ##1F2335    #yellow and base02
set -g message-style "fg=#{@primary_color},bright"
set -g message-command-style "fg=#{@command_color},bright"

set-window-option -g window-status-style fg=default,bg=default
set-window-option -g window-status-current-style "fg=default,bg=#{@active_color},bold"
set-window-option -g window-status-activity-style fg=default,bg=default,bold

set -g status-left '#S|#{?client_prefix,b, }'
#status-left-length 10
#status-left-style default
set -g window-status-format ' #I:#W#F '
set -g window-status-current-format ' #I:#W#F '
#set -g window-status-format " #I:#W#{?#{==:#F,}, ,#F} "
#set -g window-status-current-format " #I:#W#{?#{==:#F,}, ,#F} "

# set -g window-status-separator " "
set -g status-right '      '
#status-right-length 40
#set -g status-right ' %I:%M     '
#set -g status-right '#H     '

set-option -g pane-border-style fg=default
set-option -Fg pane-active-border-style "fg=#{@primary_color}"
set-option -Fg display-panes-active-colour "#{@panes_active_color}"
set-option -Fg display-panes-colour "#{@panes_color}"

set-window-option -Fg clock-mode-colour "#{@primary_color}"
set-window-option -g window-status-bell-style "fg=#{@bell_fg_color},bg=#{@bell_bg_color}"
